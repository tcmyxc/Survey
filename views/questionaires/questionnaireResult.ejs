<!DOCTYPE html>
<html lang="zh-CN">

<head>
    <% include ../components/headElements.html %>
</head>

<body>
    <% include ../components/header.html %>
    <div class="container-fluid">
        <div class="row">
            <% include ../components/sidebar.html %>
            <!-- 网站主要内容存放的地方 -->
            <div class="col-sm-10 col-sm-offset-2 col-md-10 col-md-offset-2 main">
                <h1 class="page-header">问卷网站</h1>
                <% if(questionnaireTitle && questionnaireDesc) { %>
                <div style="border: 1px solid rgba(86,61,124,.2); margin-bottom: 10px; padding: 10px;
}">
                    <h1 class="text-center">
                        <%=questionnaireTitle %>
                    </h1>
                    <p>
                        <%=questionnaireDesc %>
                    </p>
                </div>
                 <% } %>
                <div class="row">
                    <% if(data.length == 0){ %>
                    <h1>暂时无人填写表单</h1>
                    <% } %>
                    <% if(data.length > 0){ %>
                    <table class="table table-striped table-condensed table-bordered">
                        <thead>
                            <tr>
                                <th>#</th>
                                <%# 遍历JSON对象的属性，然后作为表格的列属性 %>
                                <%var desc = JSON.parse(data[0]) %>
                                <% for(var key in desc){ %>
                                <th>
                                    <%=key %>
                                </th>
                                <% } %>
                            </tr>
                        </thead>
                        <tbody>
                            <% for(var i=0; i<data.length; i++){ %>
                            <tr>
                                <th scope="row"><%= i+1 %></th>
                                <%# 获取JSON数据的值 %>
                                <%var desc = JSON.parse(data[i]) %>
                                <% for(var key in desc){ %>
                                <th>
                                    <%=desc[key] %>
                                </th>
                                <% } %>
                            </tr>
                            <% } %>
                        </tbody>
                    </table>
                    <% } %>
                </div>
            </div>
        </div>
    </div>
</body>

</html>